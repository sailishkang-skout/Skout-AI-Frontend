# Skout AI Frontend — local Docker
# Prerequisite: backend API running on host or in Docker (port 3001)
#
# Usage:
#   docker compose up --build
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up --build

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:3001}
    ports:
      - "${WEB_PORT:-3000}:3000"
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:3001}
      NODE_ENV: production
    extra_hosts:
      # Allows container to reach API on host when backend runs via `pnpm dev`
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3000/"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
