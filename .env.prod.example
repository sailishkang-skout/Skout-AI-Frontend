# Skout AI Frontend — PRODUCTION template (coming soon)
# Branch: main

NEXT_PUBLIC_API_URL=https://REPLACE_WITH_PROD_API_DOMAIN
# Native CRM service shares the same ALB as the main API (path-based routing) — same value.
NEXT_PUBLIC_CRM_API_URL=https://REPLACE_WITH_PROD_API_DOMAIN
