# Skout AI Frontend — DEV template
# Copy: cp .env.dev.example .env.dev
# Branch: develop

NEXT_PUBLIC_API_URL=http://REPLACE_WITH_DEV_ALB_DNS
# Native CRM service shares the same ALB as the main API (path-based routing) — same value.
NEXT_PUBLIC_CRM_API_URL=http://REPLACE_WITH_DEV_ALB_DNS

# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
