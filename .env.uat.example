# Skout AI Frontend — UAT / sandbox template (coming soon)
# Branch: uat

NEXT_PUBLIC_API_URL=http://REPLACE_WITH_UAT_ALB_DNS
# Native CRM service shares the same ALB as the main API (path-based routing) — same value.
NEXT_PUBLIC_CRM_API_URL=http://REPLACE_WITH_UAT_ALB_DNS
