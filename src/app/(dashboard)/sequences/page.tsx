import { PlaceholderPage } from "@/components/placeholder-page";

export default function SequencesPage() {
  return (
    <PlaceholderPage
      title="Sequence Builder"
      description="Multi-step outbound campaigns — Temporal workflows per enrolled contact."
      apiPath="/api/v1/sequences"
    />
  );
}
